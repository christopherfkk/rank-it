enum NotifSocketEvent {
  LatestNotifications = 'latest_notifications',
  RequestNotifications = 'request_notifications',
  // SendAllMessages = 'send_all_messages',
  // ReceiveMessage = 'receive_message'
}

export default NotifSocketEvent;
